name: rtsp_mixer
description: "Roomtone — a baby monitor that mixes audio from two Unifi Protect cameras and keeps listening all night."
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.
version: 1.11.0+17

environment:
  sdk: ^3.9.2

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter

  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^1.0.8
  flutter_riverpod: ^3.3.1
  dio: ^5.9.2
  flutter_secure_storage: ^10.2.0
  go_router: ^17.2.3
  riverpod_annotation: ^4.0.2
  media_kit: ^1.2.6
  media_kit_video: ^2.0.1
  media_kit_libs_video: ^1.0.6
  media_kit_native_event_loop: ^1.0.9
  flutter_foreground_task: ^9.2.2
  audio_service: ^0.18.18
  connectivity_plus: ^7.1.1
  flutter_local_notifications: ^21.0.0
  path_provider: ^2.1.5
  package_info_plus: ^10.1.0
  url_launcher: ^6.3.2

dev_dependencies:
  flutter_test:
    sdk: flutter

  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices. The lint set provided by the package is
  # activated in the `analysis_options.yaml` file located at the root of your
  # package. See that file for information about deactivating specific lint
  # rules and activating additional ones.
  flutter_lints: ^6.0.0
  build_runner: ^2.15.0
  mockito: ^5.6.4
  fake_async: ^1.3.0
  # Branding pipeline. Sources are generated by tool/branding/generate_mark.py
  # and gated by tool/branding/verify_assets.py; these two turn them into
  # platform launcher icons and splash screens.
  flutter_launcher_icons: ^0.14.4
  flutter_native_splash: ^2.4.6

# Launcher icons — regenerate with:
#   dart run flutter_launcher_icons
flutter_launcher_icons:
  android: true
  ios: false
  image_path: "assets/branding/mark-1024.png"
  # Adaptive icon: the launcher masks the foreground layer to whatever shape
  # the device uses, so the mark is inset and the petrol ground is a flat
  # colour rather than baked into the artwork.
  adaptive_icon_background: "#0B1618"
  adaptive_icon_foreground: "assets/branding/mark-foreground-1024.png"
  # Android 13+ themed icons: system tints this single-colour layer.
  adaptive_icon_monochrome: "assets/branding/mark-monochrome-1024.png"
  remove_alpha_ios: true

# Splash screen — regenerate with:
#   dart run flutter_native_splash:create
flutter_native_splash:
  # Roomtone ground per brightness: light grey in light, petrol in dark.
  color: "#EBF0EF"
  color_dark: "#0B1618"
  image: "assets/branding/mark-splash-light.png"
  image_dark: "assets/branding/mark-splash-dark.png"
  android: true
  ios: false
  web: false
  android_12:
    color: "#EBF0EF"
    color_dark: "#0B1618"
    image: "assets/branding/mark-splash-light.png"
    image_dark: "assets/branding/mark-splash-dark.png"

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  # Bundle the repo-root CHANGELOG.md so the About screen can load it at
  # runtime via rootBundle.loadString('CHANGELOG.md').
  assets:
    - CHANGELOG.md

  # Typography is VENDORED, never fetched at run time: a nursery with flaky
  # WiFi still has to render text. Verify with tool/branding/verify_fonts.py.
  fonts:
    # Display / headlines — the app's voice.
    - family: Outfit
      fonts:
        - asset: assets/fonts/Outfit-SemiBold.ttf
          weight: 600
    # UI text — titles, body, labels.
    - family: Nunito Sans
      fonts:
        - asset: assets/fonts/NunitoSans-Regular.ttf
          weight: 400
        - asset: assets/fonts/NunitoSans-SemiBold.ttf
          weight: 600
        - asset: assets/fonts/NunitoSans-Bold.ttf
          weight: 700
    # Numerics — uptime, levels, bitrates, timestamps (tabular figures).
    - family: Roboto Mono
      fonts:
        - asset: assets/fonts/RobotoMono-Regular.ttf
          weight: 400
        - asset: assets/fonts/RobotoMono-Medium.ttf
          weight: 500

  # To add assets to your application, add an assets section, like this:
  # assets:
  #   - images/a_dot_burr.jpeg
  #   - images/a_dot_ham.jpeg

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/asset-from-package

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/to/font-from-package
