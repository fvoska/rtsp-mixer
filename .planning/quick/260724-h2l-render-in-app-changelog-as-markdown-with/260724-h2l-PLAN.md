---
phase: quick-260724-h2l
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/features/about/changelog.dart
  - lib/features/about/screens/about_screen.dart
  - test/features/about/changelog_test.dart
autonomous: true
requirements: [QUICK-260724-h2l]

must_haves:
  truths:
    - "The About screen renders the changelog as structured releases, one collapsible section per version, all collapsed by default."
    - "Tapping a release header expands it to reveal that version's grouped bullets (Features / Bug Fixes / Documentation) with bold scope prefixes and tappable-looking link text."
    - "A malformed or empty changelog degrades to plain selectable text (or the existing 'not available' line) instead of throwing — the page always renders."
    - "The 'Made by' card body shows only the name and the email (no repeated 'Made by' prefix); the card title stays 'Made by'."
  artifacts:
    - "lib/features/about/changelog.dart — pure parser + release model for the release-please subset."
    - "lib/features/about/screens/about_screen.dart — collapsible release rendering + trimmed 'Made by' body."
    - "test/features/about/changelog_test.dart — unit tests for the parser incl. malformed fallback."
  key_links:
    - "about_screen.dart changelog FutureBuilder → parseChangelog() → collapsible release widgets; empty parse result → plain-text fallback."
---

<objective>
Render the in-app changelog (About screen) as structured markdown: one collapsible/expandable section per release, all collapsed by default, matching the Help screen's accordion pattern. Also trim the "Made by" card body so it shows just the name and email (no duplicated "Made by").

Purpose: The current raw monospace dump of CHANGELOG.md is hard to scan. Structured, collapsed-by-default releases make version history scannable while keeping the page dependency-free and crash-proof per CLAUDE.md.

Output: A dependency-free changelog parser for the fixed release-please subset, collapsible release rendering wired into AboutScreen, a trimmed "Made by" body, and parser unit tests.
</objective>

<execution_context>
@/home/user/rtsp-mixer/.claude/gsd-core/workflows/execute-plan.md
@/home/user/rtsp-mixer/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@CLAUDE.md
@lib/features/about/screens/about_screen.dart
@lib/features/help/screens/help_screen.dart
@CHANGELOG.md

# Reference only — do NOT add a markdown renderer package. No new dependency (see about_screen.dart:126, and flutter_markdown is discontinued upstream). Parse the fixed release-please subset by hand and render with native widgets.
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Parse the release-please changelog subset into a release model</name>
  <files>lib/features/about/changelog.dart, test/features/about/changelog_test.dart</files>
  <behavior>
    - Given the real CHANGELOG.md text, parseChangelog returns an ordered list of releases, newest first, each with: version string, optional compare URL, optional date, and an ordered list of sections.
    - Each section has a heading (e.g. Features, Bug Fixes, Documentation) and an ordered list of entries; each entry preserves its raw bullet text (the parser does NOT need to strip bold/link markdown — the renderer handles inline spans in Task 2, or the renderer may consume raw text).
    - The top "# Changelog" H1 header is ignored (not treated as a release).
    - Empty input returns an empty list.
    - Input that contains no "## [" release headers returns an empty list (signals the caller to use the plain-text fallback) rather than throwing.
    - Malformed release lines that do not match the expected shape are skipped defensively, not thrown on.
    - A release header with no following section/bullets yields a release with an empty sections list (still rendered, just empty body).
  </behavior>
  <action>Create lib/features/about/changelog.dart with immutable model classes (a release: version, compareUrl, date, sections; a section: heading, entries; an entry may just be a raw String or a small struct — keep it minimal) and a top-level pure function parseChangelog(String) that splits on release headers matching the release-please pattern (## [x.y.z](url) (YYYY-MM-DD)), then groups ### section headers and their `*`/`-` bullet lines under each release. Do NOT add any package dependency. Keep parsing tolerant: wrap the whole body in defensive handling so any unexpected line is skipped rather than fatal, and any total parse miss returns an empty list so the screen can fall back to plain text. Do NOT inline the fenced regex here — implement in the file. Add test/features/about/changelog_test.dart covering: the real changelog shape (multiple releases, multiple section types), empty string, non-release text, and a release with an empty body.</action>
  <verify>
    <automated>flutter test test/features/about/changelog_test.dart</automated>
  </verify>
  <done>parseChangelog returns correctly ordered, correctly grouped releases for real changelog text; empty/non-matching/malformed inputs return an empty list or degrade without throwing; all unit tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Render collapsible releases in AboutScreen and trim the "Made by" body</name>
  <files>lib/features/about/screens/about_screen.dart</files>
  <action>In the changelog FutureBuilder branch, replace the raw monospace SelectableText with structured rendering: call parseChangelog on the loaded text; if it returns a non-empty list, render each release inside its own collapsible section using an ExpansionTile-in-Card matching the Help screen's _HelpSection pattern (shape: Border(), collapsedShape: Border(), collapsed by default via initiallyExpanded defaulting to false), with the version (and date) as the tile title and the grouped sections/bullets as the expanded children — render bold scope prefixes (`**...**`) and `[text](url)` links as styled inline spans (RichText/Text.rich); link spans should be visually distinguished (primary color) — a tap handler/launch is out of scope, styling only. If parseChangelog returns an empty list (malformed/unexpected shape), fall back to the existing plain SelectableText monospace rendering so the page never loses the changelog; keep the existing null/empty and Loading states unchanged. Keep the outer Changelog _Section card/header as-is (the releases become the collapsible items inside, or wrap the list directly — match Help visual weight). Separately, in the "Made by" _Section, change the body's first Text from 'Made by Filip Voska' to just 'Filip Voska' (name only) so it no longer repeats the card title; leave the SelectableText email untouched.</action>
  <verify>
    <automated>flutter analyze lib/features/about/screens/about_screen.dart lib/features/about/changelog.dart</automated>
  </verify>
  <done>About screen renders one collapsible section per release, all collapsed by default, with grouped bullets and styled bold/link spans; malformed changelog falls back to plain text; the "Made by" body reads only the name plus email; flutter analyze reports no new issues.</done>
</task>

</tasks>

<verification>
- `flutter test test/features/about/changelog_test.dart` passes (parser + fallback behavior).
- `flutter analyze lib/features/about` reports no errors.
- Manual smoke (optional): open Settings → About; each release is a collapsed card, expandable one at a time; "Made by" shows name + email only.
- No new package added to pubspec.yaml (grep confirms no flutter_markdown / markdown dependency).
</verification>

<success_criteria>
- Changelog renders as collapsible per-release sections, all collapsed by default, matching the Help accordion visual pattern.
- Bold scope prefixes and links render as styled inline spans.
- Malformed/empty changelog degrades to plain text without throwing.
- "Made by" card body shows only name + email; title unchanged.
- No new dependency introduced.
</success_criteria>

<output>
Create `.planning/quick/260724-h2l-render-in-app-changelog-as-markdown-with/260724-h2l-SUMMARY.md` when done.
</output>
