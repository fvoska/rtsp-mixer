---
phase: quick-260724-83t
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pubspec.yaml
  - lib/features/about/screens/about_screen.dart
  - lib/core/router/app_router.dart
  - lib/features/settings/screens/settings_screen.dart
autonomous: true
requirements:
  - QUICK-260724-83t

must_haves:
  truths:
    - "From Settings, tapping 'About' opens a detail page with its own AppBar + back button (same pattern as Help)."
    - "The About page shows the app version read at runtime via package_info_plus."
    - "The About page shows the bundled CHANGELOG.md contents, or a graceful fallback message if it cannot be read."
    - "The About page opens the standard Flutter open-source license list."
    - "The About page shows 'made by Filip Voska' and the contact email filip.voska@infinum.com."
    - "No failure to load version or changelog can crash the app or the page."
  artifacts:
    - "lib/features/about/screens/about_screen.dart"
    - "pubspec.yaml (package_info_plus dependency + CHANGELOG.md asset)"
  key_links:
    - "app_router.dart registers /about as a top-level route above the shell"
    - "settings_screen.dart pushes /about via a ListTile entry point"
    - "rootBundle.loadString('CHANGELOG.md') resolves because CHANGELOG.md is declared under flutter/assets"
---

<objective>
Add an "About" page to the app exposing open-source licenses, app version, changelog, and author/contact info, wired into navigation from the Settings tab.

Purpose: Give users a standard, trustworthy About surface (version + changelog + licenses + attribution) matching common mobile-app conventions.
Output: A new About screen, a `/about` route, a Settings entry point, and the pubspec changes (explicit `package_info_plus` dependency + `CHANGELOG.md` bundled as an asset).
</objective>

<execution_context>
@/home/user/rtsp-mixer/.claude/gsd-core/workflows/execute-plan.md
@/home/user/rtsp-mixer/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@CLAUDE.md

# Existing patterns to match exactly:
@lib/core/router/app_router.dart          # top-level detail routes (/help, /sessions/:id) live ABOVE the StatefulShellRoute
@lib/features/help/screens/help_screen.dart   # static detail screen: Scaffold + AppBar(title) + SingleChildScrollView + ConstrainedBox(maxWidth: 640) + Spacing.*
@lib/features/settings/screens/settings_screen.dart  # entry-point pattern: ListTile(leading: Icon, title, subtitle, onTap: () => context.push('/help')) then Divider before Sign out
@lib/core/theme/spacing.dart              # Spacing.xs/sm/md/lg/xl constants

# Notes discovered during planning (do NOT re-verify — act on these):
# - package_info_plus is ONLY a transitive dep (present in pubspec.lock, absent from pubspec.yaml). It MUST be added explicitly to use it directly.
# - pubspec.yaml has NO `assets:` section yet (only `uses-material-design: true`). CHANGELOG.md lives at repo root next to pubspec.yaml.
# - url_launcher and a markdown renderer are NOT dependencies. Do NOT add them. Show the email as selectable text; show the changelog as plain selectable text.
# - App display name is "RTSP Mixer". Version field in pubspec.yaml is `version: 1.8.0+11`.
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Add deps/assets and build the About screen end-to-end</name>
  <files>pubspec.yaml, lib/features/about/screens/about_screen.dart</files>
  <action>
    In pubspec.yaml: (a) add `package_info_plus:` to the dependencies block, pinning a caret range compatible with the version already resolved in pubspec.lock (read the resolved version from pubspec.lock around the `package_info_plus:` entry and use `^<that-version>`). (b) Add an `assets:` list under the existing `flutter:` section (which currently only has `uses-material-design: true`) with a single entry `- CHANGELOG.md`, so the repo-root CHANGELOG.md is bundled and loadable at runtime on Android via rootBundle.

    Create lib/features/about/screens/about_screen.dart as a StatelessWidget `AboutScreen` following the HelpScreen layout convention exactly: `Scaffold(appBar: AppBar(title: const Text('About')), body: SingleChildScrollView(padding: symmetric(horizontal: Spacing.lg, vertical: Spacing.lg), child: Center(child: ConstrainedBox(constraints: BoxConstraints(maxWidth: 640), child: Column(crossAxisAlignment: stretch, ...)))))`. Use the `Spacing.*` constants and `Theme.of(context)` text/color styles matching HelpScreen. Sections in order:
      1. Version — a FutureBuilder over `PackageInfo.fromPlatform()`. While waiting show a subtle placeholder; on data show "Version {version} (build {buildNumber})"; on error show "Version unavailable" (do NOT rethrow). Wrap the future call so any failure resolves to the unavailable state rather than throwing — the page must render even if package_info fails.
      2. Made by — attribution text "Made by Filip Voska" and the contact email `filip.voska@infinum.com` presented via `SelectableText` (so the user can copy it; do NOT add url_launcher).
      3. Changelog — a FutureBuilder over `rootBundle.loadString('CHANGELOG.md')` wrapped so a load failure resolves to null instead of throwing. On success render the raw text inside a `SelectableText` (plain text; there is no markdown renderer). On null/empty show a graceful fallback line such as "Changelog is not available." Keep it inside the scroll view (do not nest a second scrollable).
      4. Open-source licenses — a `ListTile`/button that calls `showLicensePage(context: context, applicationName: 'RTSP Mixer', applicationVersion: <version-string-if-available-else-omitted>)`.

    Per CLAUDE.md defensive rules: every runtime read (package info, asset load) must degrade gracefully — the page must never throw and never crash the app.
    Do NOT put fenced code in comments; keep comments minimal and behavioral.
  </action>
  <verify>
    <automated>flutter pub get && flutter analyze lib/features/about/screens/about_screen.dart</automated>
  </verify>
  <done>package_info_plus is an explicit dependency, CHANGELOG.md is declared as a Flutter asset, `flutter pub get` succeeds, and AboutScreen analyzes clean with version/changelog/licenses/attribution sections that all degrade gracefully.</done>
</task>

<task type="auto">
  <name>Task 2: Register /about route and add the Settings entry point</name>
  <files>lib/core/router/app_router.dart, lib/features/settings/screens/settings_screen.dart</files>
  <action>
    In app_router.dart: import the new AboutScreen and register a top-level `GoRoute(path: '/about', builder: (_, _) => const AboutScreen())` ABOVE the `StatefulShellRoute.indexedStack` (place it next to the existing `/help` route so it stacks as a normal detail page with its own AppBar + back button, matching the documented pattern in that file). Do not place it inside a shell branch.

    In settings_screen.dart: add a `ListTile(leading: const Icon(Icons.info_outline), title: const Text('About'), subtitle: const Text('Version, changelog, licenses, and credits.'), onTap: () => context.push('/about'))` immediately after the existing "Help & setup guides" ListTile, with a `const Divider(height: 1)` separating it, keeping the "Sign out / Reset setup" ListTile last. Match the surrounding ListTile style exactly.
  </action>
  <verify>
    <automated>flutter analyze lib/core/router/app_router.dart lib/features/settings/screens/settings_screen.dart</automated>
  </verify>
  <done>Settings shows an "About" ListTile above Sign out that pushes /about; /about is a top-level route rendering AboutScreen with its own AppBar + back button; both files analyze clean.</done>
</task>

</tasks>

<verification>
- `flutter pub get` succeeds with package_info_plus added.
- `flutter analyze lib` reports no new errors in the changed files.
- Manual: Settings → About opens the page; version shows a value (or "unavailable"), changelog text renders (or fallback), "Open-source licenses" opens the license list, and "Made by Filip Voska / filip.voska@infinum.com" is visible and selectable.
</verification>

<success_criteria>
- About page reachable from Settings with a back button (Help pattern).
- Version via package_info_plus, changelog via bundled CHANGELOG.md, licenses via showLicensePage, attribution + contact shown.
- No load failure can crash the app or the page (defensive per CLAUDE.md).
</success_criteria>

<output>
Create `.planning/quick/260724-83t-add-about-page-with-licenses-version-inf/260724-83t-SUMMARY.md` when done
</output>
