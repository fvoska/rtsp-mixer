---
phase: quick-260725-ev6
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/core/theme/app_theme.dart
  - lib/core/theme/status_colors.dart
  - lib/core/theme/app_typography.dart
  - lib/app.dart
  - lib/core/providers/settings_provider.dart
  - lib/core/services/foreground_service.dart
  - lib/features/settings/screens/settings_screen.dart
  - lib/features/monitoring/screens/monitoring_screen.dart
  - lib/features/monitoring/screens/health_summary_screen.dart
  - lib/features/monitoring/widgets/camera_audio_card.dart
  - lib/features/monitoring/widgets/active_session_bar.dart
  - pubspec.yaml
  - assets/fonts/
  - assets/branding/
  - tool/branding/
  - android/app/src/main/res/
  - test/core/theme/app_theme_test.dart
  - test/core/theme/status_colors_test.dart
  - test/core/providers/settings_provider_test.dart
  - test/features/monitoring/widgets/camera_audio_card_test.dart
  - test/features/monitoring/widgets/camera_audio_card_layout_test.dart
  - test/features/monitoring/widgets/active_session_bar_test.dart
  - test/features/monitoring/screens/log_screen_test.dart
  - test/features/monitoring/screens/health_summary_screen_test.dart
autonomous: true
requirements: [QUICK-260725-ev6]

must_haves:
  truths:
    - "The app has a real light theme and a real dark theme built from the locked Roomtone petrol/teal palette (D-01), not a seeded Material default."
    - "A user can pick System / Light / Dark in Settings, the whole app switches immediately, and the choice survives an app restart (D-03)."
    - "Status colours (live / connecting / reconnecting / offline / warning) resolve per-brightness off the active theme, so nothing renders low-contrast green-on-white in light mode (D-02)."
    - "Typography is Outfit (display) + Nunito Sans (UI) + Roboto Mono (numerics), loaded from TTFs vendored in the repo — the app never fetches a font over the network at runtime (D-04)."
    - "Every numeric readout (uptime, level, bitrate, timestamps) uses tabular figures so digits do not jitter as they tick (D-04)."
    - "Monitor screen and camera cards carry the Nightwatch shape — 20px radii, roomier padding, 48-56dp touch targets, ~17px slider thumbs, a soft teal activity halo, and a calm 'Listening - 6h 12m' header status line (D-05)."
    - "Every pre-existing control still exists and still works: volume slider, pan slider, mute, quality selector, video preview toggle with zoom/pan, remove-camera, debug info panel, source badge, reconnecting/error banners (D-05)."
    - "The launcher icon is the crescent level-meter mark on petrol; the stock Flutter icon is gone from every android mipmap folder; the splash matches the theme in both light and dark (D-06)."
    - "No new presentation code can kill a running audio stream — the pulsing status dot, the activity halo, the uptime ticker, and font/theme resolution all degrade silently on failure rather than throwing."
    - "`flutter analyze` is clean and `flutter test` passes; tests that pinned the old theme/settings shape are updated, not deleted."
  artifacts:
    - "lib/core/theme/app_theme.dart — explicit AppTheme.light and AppTheme.dark from the locked palette, AppBar pinning retained in both."
    - "lib/core/theme/status_colors.dart — StatusColors ThemeExtension (live/connecting/reconnecting/offline/warning) with light+dark instances and a single-lookup accessor."
    - "lib/core/theme/app_typography.dart — TextTheme builder over the vendored families, with tabular-figure numeric styles."
    - "assets/fonts/*.ttf — 6 verified-real static TTFs (Outfit 600; Nunito Sans 400/600/700; Roboto Mono 400/500)."
    - "assets/branding/ — SVG master of the crescent mark plus generated source PNGs (full-colour, monochrome, background)."
    - "tool/branding/ — the reproducible generator script for the mark PNGs."
    - "android/app/src/main/res/mipmap-* — regenerated launcher icons; adaptive + monochrome layers."
    - "test/core/theme/status_colors_test.dart — per-brightness resolution and contrast-sanity coverage."
  key_links:
    - "AppSettings.themeMode -> settingsProvider -> MaterialApp.router theme/darkTheme/themeMode in lib/app.dart."
    - "StatusColors ThemeExtension -> Theme.of(context) lookup -> camera card dot/halo/banner, monitor screen banners, health summary, active session bar."
    - "assets/fonts/*.ttf -> pubspec.yaml fonts: declaration -> AppTypography TextTheme -> both ThemeData instances."
    - "Session.startedAt (sessionHistoryProvider.current) -> Monitor header status line uptime in tabular mono."
    - "assets/branding SVG/PNG -> flutter_launcher_icons + flutter_native_splash -> android res mipmaps/drawables + foreground-service notification icon."
---

<objective>
Replace the app's stock Material look with a deliberate visual identity: the "Nightwatch" layout/shape on the "Roomtone" petrol/teal palette, full light+dark theming with a user-selectable theme mode, vendored custom typography, and a real logo + splash in place of the default Flutter icon.

Purpose: this is a baby monitor a parent stares at in a dark room at 3am and trusts while asleep. The current UI is a seeded Material 3 dark theme with the default Flutter launcher icon — it reads as unfinished and it has no light mode at all. The redesign gives it a calm, legible, owned identity in both brightnesses.

Output: a restyled presentation layer (theme, typography, shape), a persisted `themeMode` setting, and a generated icon/splash pipeline. Audio, RTSP, reconnect, and watchdog logic are untouched.
</objective>

<execution_context>
@/home/user/rtsp-mixer/.claude/gsd-core/workflows/execute-plan.md
@/home/user/rtsp-mixer/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/STATE.md
@lib/core/theme/app_theme.dart
@lib/core/theme/spacing.dart
@lib/app.dart
@lib/core/providers/settings_provider.dart
@lib/features/settings/screens/settings_screen.dart
@lib/features/monitoring/widgets/camera_audio_card.dart
@lib/features/monitoring/screens/monitoring_screen.dart
@pubspec.yaml

## Environment facts already verified during planning — do not re-discover

- No SVG rasterizer is installed: `rsvg-convert`, `inkscape`, `convert`, and `magick` are all absent, and `cairosvg` is not importable.
- `python3` is present and `python3 -m pip install pillow` succeeds (Pillow 12.3.0 installed cleanly through the proxy). **Pillow is the sanctioned rasterization path for Task 5.**
- `https://fonts.gstatic.com/...` returns 200 and `https://pub.dev/api/packages/...` returns 200, so both font downloads and new pub dev-dependencies work here.
- Android launcher assets today: `android/app/src/main/res/mipmap-{mdpi,hdpi,xhdpi,xxhdpi,xxxhdpi}/ic_launcher.png` at 48/72/96/144/192 px, plus `res/drawable/launch_background.xml`, `res/drawable-v21/launch_background.xml`, `res/values/styles.xml`, `res/values-night/`.
- The stock Flutter launcher PNGs currently in those five folders have these md5 sums, smallest density first: `6270344430679711b81476e29878caa7`, `13e9c72ec37fac220397aa819fa1ef2d`, `a0a8db5985280b3679d99a820ae2db79`, `afe1b655b9f32da22f9a4301bb8e6ba8`, `57838d52c318faff743130c3fcfae0c6`. Task 5's verification asserts none of them survive.

## Call sites you will have to touch (exhaustive, from a repo-wide grep)

The three fixed status-colour constants on `AppTheme` are referenced at:
- `lib/features/monitoring/widgets/camera_audio_card.dart` lines 134, 198, 200, 496, 541, 543, 557, 643, 647, 713, 736
- `lib/features/monitoring/screens/monitoring_screen.dart` lines 284, 285, 286, 623, 624, 928, 929, 1101, 1102
- `lib/features/monitoring/screens/health_summary_screen.dart` lines 439, 442
- `lib/features/monitoring/widgets/active_session_bar.dart` line 105
- `test/features/monitoring/widgets/camera_audio_card_test.dart` line 206

`AppTheme.dark` is referenced at `lib/app.dart:88` and in six test files:
`test/core/theme/app_theme_test.dart`, `test/features/monitoring/screens/log_screen_test.dart:11`,
`test/features/monitoring/screens/health_summary_screen_test.dart:52`,
`test/features/monitoring/widgets/camera_audio_card_test.dart:15`,
`test/features/monitoring/widgets/active_session_bar_test.dart:53`,
`test/features/monitoring/widgets/camera_audio_card_layout_test.dart:32`.

`AppSettings` is asserted in `test/core/providers/settings_provider_test.dart`.

## Useful existing patterns to reuse rather than reinvent

- `ActiveSessionBar` (`lib/features/monitoring/widgets/active_session_bar.dart`) already implements a 1-second `Timer.periodic` + `AnimatedOpacity` pulsing live dot and a live-ticking uptime label. Task 4's Monitor header status line should follow that exact pattern.
- The live session's start time is `ref.watch(sessionHistoryProvider).value?.current?.startedAt`.
- `health_summary_screen.dart` already has a private `_formatUptime(Duration)`; if Task 4 needs the same "6h 12m" shape, lift it to a shared pure helper rather than copying it.
- `Spacing` (`lib/core/theme/spacing.dart`) is the spacing scale — xs 4 / sm 8 / md 16 / lg 24 / xl 32 / xxl 48. Extend it if Nightwatch needs a new step; do not hardcode ad-hoc paddings.
- `camera_audio_card.dart` already animates state changes with `AnimatedSize` + fade-only `AnimatedSwitcher` at 250ms. Match those durations for anything new.

## Hard constraints (from CLAUDE.md, binding)

- **Defensive error handling.** This is a baby monitor. No exception introduced by this restyle may reach a build method, a timer callback, or a stream listener in a way that can tear down audio. Anything new and non-critical — the pulsing dot, the halo, the uptime ticker, the theme-extension lookup, asset/font resolution — must fail into a plain fallback and log via `appLog`, never throw.
- **Presentation only.** Do not touch audio, RTSP, reconnect, or watchdog logic. The only non-presentation changes allowed are the new `themeMode` settings field and the icon/splash pipeline.
- **Conventional Commits** for every commit.
- **Restyle, not removal.** Every existing control and capability must survive Task 4 intact.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Roomtone light+dark themes and a StatusColors ThemeExtension</name>
  <files>lib/core/theme/app_theme.dart, lib/core/theme/status_colors.dart, lib/features/monitoring/widgets/camera_audio_card.dart, lib/features/monitoring/widgets/active_session_bar.dart, lib/features/monitoring/screens/monitoring_screen.dart, lib/features/monitoring/screens/health_summary_screen.dart, test/core/theme/app_theme_test.dart, test/core/theme/status_colors_test.dart, test/features/monitoring/widgets/camera_audio_card_test.dart</files>
  <behavior>
    - `AppTheme.light.brightness` is light and `AppTheme.dark.brightness` is dark.
    - Both themes expose a non-null `StatusColors` extension through `Theme.of(context)`.
    - The live colour differs between light and dark (dark 0xFF3FBFAD, light 0xFF0B8578).
    - AppBar pinning survives in BOTH themes: background equals colorScheme.surface, foreground equals onSurface, surfaceTint transparent, scrolledUnderElevation 0, elevation 0, centerTitle false.
    - The accessor used at call sites never returns null, even against an ad-hoc ThemeData with no extension registered.
  </behavior>
  <action>
Implement D-01 and D-02.

Create `lib/core/theme/status_colors.dart` holding a `StatusColors` class extending `ThemeExtension` over itself, with five final `Color` fields — `live`, `connecting`, `reconnecting`, `offline`, `warning` — plus the required `copyWith` and `lerp` overrides. Declare two `static const` instances, one dark and one light, populated from the locked palette. Dark: live 0xFF3FBFAD, connecting 0xFF5FA8D3, warning 0xFFE0A458, offline 0xFFE0715B, with reconnecting mapping to the warning amber. Light: live 0xFF0B8578, connecting 0xFF256C93, warning 0xFF9C6220, offline 0xFFB4462F, same reconnecting mapping. Add a single-lookup accessor so call sites stay one expression — an extension getter on `BuildContext` (for example `context.statusColors`) or on `ThemeData` — which reads the extension and, if it is absent, returns the instance matching the current brightness and logs via `appLog` rather than throwing or returning null. That fallback is the defensive-error-handling rule applied to theming: an ad-hoc `ThemeData` in some future widget test must not be able to crash a card build.

Rewrite `lib/core/theme/app_theme.dart` so it exposes `AppTheme.light` and `AppTheme.dark` built from explicit `ColorScheme` instances rather than `ColorScheme.fromSeed`. Map the locked palette onto the scheme roles: ground to `scaffoldBackgroundColor` and `surfaceContainerLowest`, surface/card to `surface`, inset to `surfaceContainerHighest`, ink to `onSurface`, muted to `onSurfaceVariant`, primary/accent to `primary`, warning to `tertiary`, error to `error`, hairline to `outlineVariant` and `dividerColor`, nav to the `NavigationBarThemeData` and `NavigationRailThemeData` background so the bottom bar reads darker than the body in dark mode. Keep `useMaterial3: true`. Register the matching status-colour instance in each theme's `extensions` list. Carry the existing AppBar pinning block into BOTH themes — it exists so the M3 scrolled-under tint cannot render the same bar in two shades — and keep its explanatory comment.

Delete the three fixed status-colour statics from the `AppTheme` class and convert every call site listed in the context block to the new accessor. Nine of them are conditional expressions choosing between online and offline; keep the exact same conditions and swap only the colour source. Two call sites need care: `camera_audio_card.dart:134` derives the activity-border alpha from the online colour — keep the `(audioActivity - threshold) / (1 - threshold)` math and the `clamp(0.15, 0.9)` untouched, changing only where the base colour comes from; and `camera_audio_card.dart:543` already mixes `scheme.tertiary` with the offline colour for reconnecting — route that through the new `reconnecting` field instead.

Update `test/core/theme/app_theme_test.dart` so its six AppBar assertions run against both light and dark (a shared group or loop over the two themes), keeping the existing rationale comment. Add `test/core/theme/status_colors_test.dart` covering: both themes resolve a non-null extension; light and dark `live` differ; the fallback accessor returns a sane value for a bare `ThemeData.light()` with no extension registered; and a contrast sanity check that the light-mode live colour against the light surface 0xFFFFFFFF clears a 3:1 luminance-contrast ratio — that failing ratio on a light card is the actual defect being fixed. Fix `camera_audio_card_test.dart:206`, which asserts the progress indicator colour equals the old constant, to assert against the dark theme's live colour instead; leave its non-amber guard in place.

Commit as `refactor(theme): add Roomtone light+dark themes and StatusColors extension`.
  </action>
  <verify>
    <automated>flutter analyze --fatal-infos &amp;&amp; ! grep -rn 'AppTheme\.status' lib test &amp;&amp; flutter test test/core/theme/ test/features/monitoring/</automated>
  </verify>
  <done>`AppTheme.light` and `AppTheme.dark` both exist and both register a status-colour extension; no source or test file references the old fixed status constants; the AppBar-pinning assertions pass for both brightnesses; `flutter analyze` is clean and the monitoring plus theme tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Persisted themeMode setting wired end-to-end</name>
  <files>lib/core/providers/settings_provider.dart, lib/app.dart, lib/features/settings/screens/settings_screen.dart, test/core/providers/settings_provider_test.dart</files>
  <behavior>
    - Default `AppSettings().themeMode` is `ThemeMode.system`.
    - `copyWith(themeMode: ...)` changes only that field; `==` and `hashCode` account for it.
    - `toJson`/`fromJson` round-trip every mode; an unknown or missing persisted value falls back to system without throwing.
    - `setThemeMode` updates state and persists through the existing storage service.
    - Selecting Light in the Settings segmented control rebuilds the app under the light theme.
  </behavior>
  <action>
Implement D-03.

In `lib/core/providers/settings_provider.dart`, add a `final ThemeMode themeMode` field to `AppSettings` defaulting to `ThemeMode.system`, and thread it through the constructor, `copyWith`, `operator ==`, `hashCode`, `toJson`, and `fromJson` following the shape the three existing fields already use. Persist it as its stable string name (system / light / dark) rather than an enum index, so reordering the SDK enum can never silently flip a user's theme. `fromJson` must tolerate a missing key, a null, or an unrecognised string by returning system — decoding a settings blob written by an older build must not throw. Add `setThemeMode(ThemeMode value)` mirroring `setUseRtsp`: assign state, `appLog` the change, call `_save()`.

In `lib/app.dart`, watch `settingsProvider` inside `build` and pass `theme: AppTheme.light`, `darkTheme: AppTheme.dark`, and `themeMode: settings.themeMode` to `MaterialApp.router`. Leave the `WithForegroundTask` wrapping and the Android platform guards exactly as they are.

In `lib/features/settings/screens/settings_screen.dart`, add an Appearance section as the FIRST item in the ListView, above the "Use plain RTSP" switch: a section label styled like the existing Connection label, then a full-width three-way `SegmentedButton` over `ThemeMode` (System / Light / Dark) padded with `Spacing.lg` horizontally, whose `selected` set holds the current mode and whose `onSelectionChanged` calls `notifier.setThemeMode` with the single selection. Follow with a `Divider(height: 1)` to match the screen's existing rhythm.

Extend `test/core/providers/settings_provider_test.dart` with: the new default; a `copyWith` isolation case; a JSON round-trip across all three modes; equality and hashCode sensitivity to the field; and a malformed-payload case (unknown string and absent key both yielding system).

Commit as `feat(settings): add user-selectable light/dark/system theme mode`.
  </action>
  <verify>
    <automated>flutter analyze --fatal-infos &amp;&amp; flutter test test/core/providers/settings_provider_test.dart</automated>
  </verify>
  <done>`themeMode` is present in every `AppSettings` member (constructor, copyWith, ==, hashCode, toJson, fromJson) with a `setThemeMode` mutator; `MaterialApp.router` receives theme, darkTheme, and themeMode; Settings shows a System/Light/Dark segmented control at the top; malformed persisted values degrade to system; settings tests pass.</done>
</task>

<task type="auto">
  <name>Task 3: Vendor Outfit / Nunito Sans / Roboto Mono and build the TextTheme</name>
  <files>assets/fonts/, pubspec.yaml, lib/core/theme/app_typography.dart, lib/core/theme/app_theme.dart, lib/features/monitoring/widgets/camera_audio_card.dart, lib/features/monitoring/widgets/active_session_bar.dart, lib/features/monitoring/screens/health_summary_screen.dart</files>
  <behavior>
    - All six downloaded files begin with a real TrueType magic number and are of plausible size; a proxy error page saved with a .ttf extension fails the check loudly.
    - The typography entry point returns a TextTheme whose display and headline styles use Outfit, whose title/body/label styles use Nunito Sans, and whose numeric style uses Roboto Mono with tabular figures.
    - No package that fetches fonts over the network at runtime is added to pubspec.
  </behavior>
  <action>
Implement D-04.

Create `assets/fonts/` and download the six locked, already-verified-reachable static TTFs with `curl -fSL` — the `-f` matters, because it turns an HTTP error into a non-zero exit instead of a saved error page. Name them predictably: `Outfit-SemiBold.ttf`; `NunitoSans-Regular.ttf`, `NunitoSans-SemiBold.ttf`, `NunitoSans-Bold.ttf`; `RobotoMono-Regular.ttf`, `RobotoMono-Medium.ttf`. Copy the six URLs verbatim from the locked decision recorded in the task description — Outfit 600 under `/s/outfit/v15/`, Nunito Sans 400/600/700 under `/s/nunitosans/v19/`, Roboto Mono 400/500 under `/s/robotomono/v31/`. Do not shorten, reconstruct, or guess any of them; these exact hashed filenames were confirmed reachable during planning.

Immediately validate every downloaded file BEFORE staging anything. Write the check as a committed, re-runnable script at `tool/branding/verify_fonts.py` (create `tool/branding/`) rather than a throwaway shell line, so CI and a future contributor can re-run it: it enumerates `assets/fonts/*.ttf`, asserts there are exactly six, and asserts each is larger than 20 KB with first four bytes of `00 01 00 00` or the ASCII `true`. It prints the offending path, magic bytes, and size for any failure and exits non-zero. A downloaded HTML or JSON proxy error page starts with a different byte and fails this check. If any file fails, delete it, report the URL and the first bytes seen, and stop the task. Do not commit a placeholder and do not silently drop a family — shipping a corrupt font is not a degraded-mode scenario, it is a build that renders nothing.

Declare the three families in the `flutter:` section of `pubspec.yaml` alongside the existing `assets:` block, giving each face its weight (Outfit 600; Nunito Sans 400, 600, 700; Roboto Mono 400, 500). Do not add any dependency that downloads fonts at run time — the point of vendoring is that a nursery with flaky WiFi still renders text.

Create `lib/core/theme/app_typography.dart` exposing a single entry point that takes a `Brightness` and builds a `TextTheme` on top of `Typography.material2021()`'s base for that brightness: `displayLarge` / `displayMedium` / `displaySmall` and the three `headline` sizes in Outfit SemiBold for screen titles; the three `title`, three `body`, and three `label` sizes in Nunito Sans with sensible weights. Also expose a reusable numeric style — Roboto Mono with `fontFeatures` containing `FontFeature.tabularFigures()` — as a named getter plus a small and a large variant, so uptime, level readouts, bitrates, and timestamps stop jittering as digit widths change. Wire the result into both themes via `textTheme:`, and set `fontFamily` on each `ThemeData` to Nunito Sans so any widget that bypasses the TextTheme still inherits the right face.

Apply the numeric style to the readouts that already exist and clearly qualify: the `_StreamInfoPanel` rows in `camera_audio_card.dart` (which today hardcode a generic monospace family around line 860), and the uptime and duration text in `health_summary_screen.dart` and `active_session_bar.dart`. These are pure style swaps — do not change any of the strings or the formatting logic that produces them, and do not add error handling around them.

Commit as `feat(theme): vendor Outfit/Nunito Sans/Roboto Mono and build the app TextTheme`.
  </action>
  <verify>
    <automated>python3 tool/branding/verify_fonts.py &amp;&amp; flutter pub get &amp;&amp; flutter analyze --fatal-infos &amp;&amp; flutter test</automated>
    <human-check>Launch the app and confirm screen titles render in Outfit, body text in Nunito Sans, and that a ticking uptime no longer shifts horizontally as digits change.</human-check>
  </verify>
  <done>Six real TTFs sit in `assets/fonts/` and pass the magic-number and size check via a small committed `tool/branding/verify_fonts.py` that exits non-zero on any bad file; the families are declared in pubspec; the typography module drives both themes; existing numeric readouts use the tabular-figure style; analyze is clean and the full suite passes.</done>
</task>

<task type="auto">
  <name>Task 4: Nightwatch shape — card restyle, activity halo, and Monitor status line</name>
  <files>lib/features/monitoring/widgets/camera_audio_card.dart, lib/features/monitoring/screens/monitoring_screen.dart, lib/core/theme/app_theme.dart, lib/core/theme/spacing.dart, test/features/monitoring/widgets/camera_audio_card_layout_test.dart, test/features/monitoring/widgets/camera_audio_card_test.dart</files>
  <behavior>
    - Card corners are 20px; the inner content padding and control spacing grow to match.
    - Icon buttons and other tap targets measure at least 48dp on their shortest side; slider thumbs are ~17px.
    - With audioActivity above the threshold the card shows a teal border AND an outer glow whose intensity rises with the activity value; below the threshold both are absent.
    - The Monitor header shows a pulsing teal dot and a "Listening - 6h 12m" style uptime in tabular mono while a session is live, and shows nothing extra when idle.
    - Every existing control is still findable in a widget test: volume slider, pan slider, mute, quality selector, video toggle, zoom/pan overlay buttons, remove, debug panel, source badge, reconnecting/error banner.
  </behavior>
  <action>
Implement D-05. This is a RESTYLE. Nothing is removed.

In `camera_audio_card.dart`, raise the outer `AnimatedContainer` decoration radius from 12 to 20 and match it on the inner `Card.filled` shape so the clip and the border agree. Add a matching 20px radius wherever the card's own sub-surfaces round (the video frame at line 356 stays proportionally smaller, e.g. 12-14). Increase the content `Padding` from `Spacing.md` to `Spacing.lg` and open up the vertical gaps between the header, status line, volume row, and pan row by one step on the `Spacing` scale. Add a new step to `Spacing` if a value you need is missing rather than inlining a magic number.

Convert the activity border into the soft accent halo: keep the existing `hasActivity` predicate, the `(audioActivity - threshold) / (1 - threshold)` normalisation, and the `clamp(0.15, 0.9)` result exactly as they are — that math is tuned and stays. Feed that same clamped intensity into two things now: the existing teal `Border.all`, and a new `boxShadow` on the same `BoxDecoration` — a single `BoxShadow` in the live colour with zero offset, a blur radius that scales with intensity (roughly 8 to 28), and a spread that scales more gently (roughly 0 to 6). Because the value flows from a poll-driven state field, guard the intensity computation so a NaN, infinite, or out-of-range `audioActivity` cannot produce an invalid `BoxShadow`: sanitise to a finite value in range, and on anything unexpected fall back to no halo and `appLog` it. A silently haloless card is acceptable; an exception thrown from `build` during an overnight session is not.

Grow the touch targets: give the header action `IconButton`s and the overlay video buttons explicit `constraints` or a `SizedBox` wrapper of at least 48dp, and bump the mute control and quality selector to 48-56dp of hit area. Add a `SliderThemeData` to both themes (in `app_theme.dart`, so it applies uniformly) setting a ~17px round thumb, a thicker track, and an overlay radius sized for touch — this covers the camera card's volume and pan sliders and the Settings sliders in one place.

In `monitoring_screen.dart`, add a calm status line to the live Monitor header, placed with `_LiveToolbar` in the `_LiveMonitoringView` column (around line 409). It reads as a pulsing teal dot followed by "Listening" and the session uptime, e.g. `Listening - 6h 12m`, with the duration in the tabular-mono numeric style from Task 3. Source the start time from `sessionHistoryProvider`'s current session `startedAt`; render nothing at all when there is no current session. Copy `ActiveSessionBar`'s proven pattern for both halves: a `Timer.periodic` of one second that calls `setState` only when `mounted`, cancelled in `dispose`, with the dot's opacity driven by `AnimatedOpacity`. Wrap the duration formatting and the tick body so a null session, a future-dated `startedAt`, or a negative duration renders a neutral placeholder and logs, instead of throwing out of a timer callback. Reuse `health_summary_screen.dart`'s existing uptime formatter by lifting it into a shared pure helper rather than duplicating it.

Before committing, walk the card top to bottom and confirm the full control inventory is intact: status banner (reconnecting and error), status dot, camera name, source badge, video toggle, debug toggle, remove, status line, audio level indicator, waveform chart, volume slider with mute, pan slider, quality selector, video preview with zoom/pan/reset overlay buttons, and the stream info panel. Update the two card test files for the new geometry — the layout test asserts sizes and will need its expected radius and paddings refreshed — and add a test that a high `audioActivity` produces a decoration with a non-empty `boxShadow` while a below-threshold value produces none.

Commit as `feat(ui): apply Nightwatch shape to monitor cards and header`.
  </action>
  <verify>
    <automated>flutter analyze --fatal-infos &amp;&amp; flutter test test/features/monitoring/</automated>
    <human-check>Run the app with two cameras live. Confirm: cards have soft 20px corners with roomier spacing; making noise near a camera raises a soft teal glow that fades back down; the header shows a pulsing dot with a ticking uptime; and every control (volume, pan, mute, quality, video toggle, zoom, remove, debug) still works.</human-check>
  </verify>
  <done>Cards render at 20px radius with roomier padding and 48dp+ targets; the activity halo scales with the unchanged activity math and degrades to no halo on bad input; the Monitor header shows a pulsing dot plus tabular-mono uptime while live and nothing when idle; no control was removed; analyze is clean and the monitoring tests pass.</done>
</task>

<task type="auto">
  <name>Task 5: Crescent level-meter logo, launcher icons, splash, and notification icon</name>
  <files>assets/branding/, tool/branding/, pubspec.yaml, android/app/src/main/res/, lib/core/services/foreground_service.dart</files>
  <behavior>
    - The generator script is deterministic and re-runnable: running it twice produces byte-identical PNGs.
    - Every generated PNG is a valid image of the expected pixel dimensions and is not blank (more than one distinct colour, non-zero standard deviation).
    - No file under `android/app/src/main/res/mipmap-*` still matches any of the five recorded stock-icon md5 sums.
    - An adaptive icon XML with foreground, background, and monochrome layers exists.
    - The splash background is the petrol ground in dark and the light ground in light.
  </behavior>
  <action>
Implement D-06.

Author `assets/branding/mark.svg` as the human-readable master: on a 100x100 canvas centred at (50,50), roughly 22 rounded bars laid on an arc from -118 to +118 degrees at radius 40, each bar's length eased by `sin(t * pi)` from 7 to 24 units, stroke-width 5.4 with round caps, plus a solid r=6.5 centre dot. Teal 0xFF3FBFAD on petrol 0xFF0B1618. Also author a single-colour variant in 0xFF0F1F21 for the Android 13 themed/monochrome layer.

No SVG rasterizer exists on this machine (checked during planning: rsvg-convert, inkscape, imagemagick, and cairosvg are all absent) so take the sanctioned fallback: write `tool/branding/generate_mark.py`, a small committed Python script using Pillow (install first with `python3 -m pip install pillow`, which was confirmed to work here) that draws the same geometry numerically. Draw at 4x the target resolution and downsample with LANCZOS to get clean antialiasing, since Pillow's line primitive has no round caps — emit each bar as a thick line plus a filled circle at each endpoint. Keep the geometry constants in one block at the top of the script so the SVG and the script stay reconcilable by eye. Emit at minimum: a full-colour square source PNG at 1024px, a transparent-background foreground layer at 1024px with the mark inset to about 66 percent to survive adaptive-icon masking, and a monochrome layer at 1024px in the single-colour variant. Write them into `assets/branding/`.

Verify the generated PNGs before wiring anything. Add a committed sibling script `tool/branding/verify_assets.py` alongside the font checker: it opens each PNG under `assets/branding/` with Pillow and asserts the expected pixel dimensions, more than one distinct colour, and a non-zero per-channel standard deviation, printing the offending path and exiting non-zero on any failure. A silently blank or mis-sized icon must fail the build step, not ship.

Add `flutter_launcher_icons` and `flutter_native_splash` as dev dependencies (pub.dev is reachable) and configure both in `pubspec.yaml`. For the launcher: legacy mipmap generation from the full-colour source, `adaptive_icon_foreground` from the foreground layer, `adaptive_icon_background` as the petrol 0xFF0B1618, and `adaptive_icon_monochrome` from the monochrome layer. For the splash: `color: "#0B1618"` and `color_light: "#EBF0EF"`, with the mark as the splash image and Android 12+ configured too. Run both generators.

Then prove the stock icon is gone. Compare the md5 of every file under `android/app/src/main/res/mipmap-*` against the five recorded stock sums in the context block; any match means the generator did not overwrite that density and the task is not done. Also confirm `res/mipmap-anydpi-v26/ic_launcher.xml` exists with all three layers. If the launcher generator skips a density, regenerate that PNG directly from the script rather than leaving a stale file.

Finally set the foreground-service notification identity in `lib/core/services/foreground_service.dart`: give `AndroidNotificationOptions` (constructed at line 32) the generated small notification icon and the teal accent colour. Before writing the field names, read the installed `flutter_foreground_task` 9.2.2 source in the pub cache to confirm the exact parameter names this version exposes — this project has already been bitten once by assuming an API that version does not have (`isSticky` was removed in Phase 03). If the version exposes no such parameters, leave the call unchanged and note it in the summary rather than inventing them. Whatever you add must not change the service's start/stop behaviour.

Commit as `feat(branding): add crescent level-meter logo, adaptive icons, and themed splash`.
  </action>
  <verify>
    <automated>python3 tool/branding/generate_mark.py &amp;&amp; python3 tool/branding/verify_assets.py &amp;&amp; test -f android/app/src/main/res/mipmap-anydpi-v26/ic_launcher.xml &amp;&amp; ! md5sum android/app/src/main/res/mipmap-*/*.png | grep -qE '6270344430679711b81476e29878caa7|13e9c72ec37fac220397aa819fa1ef2d|a0a8db5985280b3679d99a820ae2db79|afe1b655b9f32da22f9a4301bb8e6ba8|57838d52c318faff743130c3fcfae0c6' &amp;&amp; flutter analyze --fatal-infos &amp;&amp; flutter test</automated>
    <human-check>Install on an Android device. Confirm the launcher shows the crescent mark (not the Flutter logo), the themed-icon setting renders the monochrome layer, the splash is petrol in dark mode and light grey in light mode, and the ongoing monitoring notification uses the new small icon.</human-check>
  </verify>
  <done>An SVG master plus a deterministic Pillow generator live in the repo; all generated PNGs pass the valid/sized/non-blank check; every stock Flutter launcher PNG has been replaced (no md5 match survives); the adaptive icon XML carries foreground, background, and monochrome layers; the splash is configured for both brightnesses; the foreground-service notification icon and accent are set against the genuinely-installed API surface; analyze is clean and the suite passes.</done>
</task>

</tasks>

<decision_coverage>
| Decision | Covered by | Notes |
|----------|-----------|-------|
| D-01 Theme system (light+dark Roomtone palette, AppBar pinning preserved in both) | Task 1 | Explicit ColorSchemes, no seeding. Typography wired in Task 3, sliders in Task 4. |
| D-02 Status colours as a ThemeExtension, every call site updated | Task 1 | 23 call sites enumerated in context; negative grep in verify proves none remain. |
| D-03 themeMode setting, persisted, wired to MaterialApp, segmented control in Settings | Task 2 | Persisted by name, not enum index. |
| D-04 Vendored typography, verified real TTFs, tabular numerals | Task 3 | Magic-number + size gate blocks a proxy error page shipping as a font. |
| D-05 Nightwatch shape: radii, padding, targets, thumbs, halo, header status line, nothing removed | Task 4 | Activity math explicitly unchanged; full control inventory re-walked before commit. |
| D-06 Logo, adaptive/monochrome icons, splash, notification icon, stock icon proven gone | Task 5 | Pillow fallback chosen because no rasterizer exists here; md5 gate proves replacement. |
</decision_coverage>

<verification>
- `flutter analyze --fatal-infos` is clean after every task.
- `flutter test` passes after every task; the CI workflow at `.github/workflows/tests.yml` runs both on the PR.
- No occurrence of the old fixed status-colour statics remains in `lib/` or `test/`.
- All six vendored fonts pass the TTF magic-number and minimum-size gate.
- No stock Flutter launcher PNG md5 survives under `android/app/src/main/res/mipmap-*`.
- Diff review confirms no file under `lib/features/monitoring/providers/`, and no reconnect, watchdog, or stream-candidate logic, was modified.
</verification>

<success_criteria>
The app launches in a coherent Roomtone identity in both light and dark, the user can switch between them from Settings and the choice sticks across a restart, text renders in the vendored families with non-jittering numerals, the Monitor screen and cards carry the Nightwatch shape with a live activity halo and a ticking header status line, and the launcher icon and splash are the crescent mark with no trace of the stock Flutter logo. Every pre-existing control still works, analyze is clean, the suite passes, and no audio path was touched.
</success_criteria>

<output>
Create `.planning/quick/260725-ev6-redesign-visual-identity-nightwatch-layo/260725-ev6-SUMMARY.md` when done.
</output>
